{
  "name": "chartkit-example-react-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm --prefix ../chartkit run build && node ./scripts/prepare-duckdb-bundles.mjs",
    "dev": "vite",
    "prebuild": "npm --prefix ../chartkit run build && node ./scripts/prepare-duckdb-bundles.mjs",
    "build": "vite build",
    "prepare:duckdb": "node ./scripts/prepare-duckdb-bundles.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@austrade/chartkit": "file:../chartkit",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.64",
    "@types/react-dom": "^18.2.21",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.8"
  }
}
